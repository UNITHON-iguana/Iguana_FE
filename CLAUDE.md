# 현장노트 AI (Iguana_FE)

건설 현장 사진으로 사진대지를 만드는 내부 업무 도구.

**본 화면은 사진대지(`/projects/:id/sheet`)다.** 사진 올리기 · AI 분석 실행 · 검수가
전부 이 한 페이지에서 끝난다. 사진을 한 장씩 열어보는 화면을 따로 두지 않는다 —
사진 한 장이 격자 한 덩어리로 눕고, 확인이 필요한 칸만 노랗게 떠서 사람은 그 칸만 본다.

**그 페이지는 두 탭으로 갈린다 — `검수`와 `사진대지`.** 하루에 수백 장이 올라와도
사람이 볼 것은 AI가 자신 없어 한 사진뿐이라, 검수 탭은 그것만 담아 끝나면 비고
사진대지 탭은 손볼 것이 없는 사진을 담는다. 두 탭은 겹치지 않고, 합치면 분석이 끝난
사진 전부다(`PhotoScope`). 별도 페이지가 아닌 이유는 검수할 때 앞뒤 사진을 함께 보는
것이 값을 고치는 근거가 되기 때문이다 — 같은 격자, 같은 조작, 담는 것만 다르다.

**올리기와 분석은 한 호출이다.** 서버가 S3 업로드·공종 목록 조회·AI 호출·저장을
`POST /api/v1/projects/{id}/photos/bulk` 한 번에 끝내고 결과까지 돌려준다. 그래서 화면에
`AI 분석 실행` 버튼이 없다 — 사진 추가가 곧 분석이고, 끝났는지 물어볼 자리도 없다.
AI는 그 프로젝트에 등록된 공종 안에서만 고른다. 목록에 없는 공종이 사진에 적혀 있으면
그 항목은 공종이 빈 채로 오고, **항목 하나만 그래도 사진 전체가** 검수로 넘어간다.
서버는 사진 단위 `needsReview` 하나만 주고 어느 칸이 미심쩍은지는 알려주지 않아,
노란 칸은 공종 칸에만 선다.

**확정 호출은 사람이 고친 사진에만 나간다.** AI가 다 맞춘 사진은 승인이 필요 없어
그대로 실적이 되고, 사람이 값을 고친 사진만 **그 사진에서 손을 뗄 때** 한 장씩
서버로 올라간다(`PATCH /api/v1/photo-uploads/{id}` — 사진에 딸린 항목 전부를 함께
보내고, 공종이 빈 항목이 하나라도 있으면 400).
확정 요청은 **항목만** 받는다(`PhotoUploadConfirmRequest`). 그래서 **위치와 작업일은
고쳐도 서버로 가지 않는다** — 화면에서 채운 값이 다시 불러오면 사라진다. 작업일이 빈 채
확정되면 집계가 날짜 열에 못 넣어 그 사진은 실적에서 조용히 빠진다. 고칠 자리가 서버에
생기기 전까지 남아 있는 구멍이다.
수량은 `rawQuantity`만 보낸다 — 서버가 규격 모양을 보고 `quantity`를 다시 계산한다
(`가로*세로`면 둘레×개수, 아니면 개수 그대로). 프론트에 그 규칙을 두지 않는다.

**타이핑 중에는 아무것도 보내지 않는다.** 엑셀이 칸을 벗어날 때 값을 확정하는 것과
같은 시점이고, 저장이 사진 단위라 칸이 아니라 사진을 경계로 잡는다
(`PhotoSheetGrid`의 `onLeave`). 시간 기준으로 저장하면 글자 사이에 잠깐 멈췄다고
중간값이 올라가 틀린 수량이 잠깐씩 집계에 잡힌다.
사진 단위 호출이라 하루에 수백 장이 올라와도 호출은 사람이 고친 만큼이다.
**확정 버튼도 체크박스도 두지 않는다** — 고친 뒤 한 번 더 누르게 하면 같은 판단을
장수만큼 두 번 시키는 셈이다. 무엇이 저장을 막는지는
`confirmBlocker`(`src/lib/workItems.ts`) 한 곳이 정하고, 이 판정은 서버가 400을 내는
조건과 같아야 한다 — 어긋나면 저장이 서버에서 되돌아온다.

**집계·엑셀·계획 대비 현황은 모두 확인할 칸이 남지 않은 사진(`needsReview`)을 센다.**
셋이 어긋나면 어느 숫자를 믿을지 알 수 없다.

**값만 저장하는 API는 아직 없다.** 그래서 확인할 칸이 남아 못 올린 편집분은
브라우저에만 있고(`SheetPage`의 `drafts`), 창을 닫으려 하면 경고가 뜬다.
임시 저장 엔드포인트가 생기면 고치는 중에도 서버에 올려둘 수 있다.

**사진대지 목록은 서버가 잘라서 준다.** 하루에 수백 장이 올라오는데 사진 한 장이 격자
4행·입력칸 16개를 차지한다. 작업일로 좁히고 한 페이지 `PHOTOS_PER_PAGE`장씩 나눈다
(`getPhotoPage`). 전부 받아오는 `getPhotos`는 집계 프로토타입만 쓰고, 집계가 서버로
옮겨가면 같이 사라진다.

집계 엑셀에는 기간 파라미터가 없다 — 화면은 한 달을 보지만 파일에는 늘 전체가 담긴다.
`SummaryPage`의 내려받기 버튼이 그 사실을 밝힌다.

집계(`summary`)와 엑셀 내보내기는 **확인할 칸이 남지 않은 사진만** 받아 쓴다.
손볼 것이 남은 사진은 합계에 섞이지 않고, 대신 몇 장이 빠져 있는지를 화면 위에 밝힌다.
**집계 계산은 서버가 한다.** 확정된 사진만 세는 것도, 규격을 갈라 행을 만드는 것도,
가로*세로를 둘레 연장으로 환산하는 것도 서버 몫이다. 프론트에는 더하기·곱하기 규칙이
없고, 받은 표를 날짜로 좁히거나 월로 접기만 한다. 자세한 것은 `docs/집계-규칙.md`.

공종(`workTypes`)은 **이름만** 등록한다. 규격은 사진마다 AI가 읽는다.
**이름은 등록한 뒤 고치지 못한다** — 서버에 수정 API가 없고 두지 않기로 했다.
잘못 등록했으면 지우고 다시 만든다. 그래서 타이핑 중에는 아무것도 보내지 않고
칸을 벗어날 때 한 번만 보낸다(`WorkTypesPage`의 `commit`) — 중간에 저장하면
`금속`처럼 덜 친 이름이 등록되고 되돌릴 방법이 없다.
계획 데이터와 계획 대비 현황은 이 흐름 없이도 돌아가는 곁가지라 한 페이지(`plan`)로 묶어
메뉴 구분선 아래에 둔다. 라우팅과 메뉴는 이 위계를 따른다
(`src/app/router.tsx`, `src/layouts/AppLayout.tsx`의 `PROJECT_MENU`).

**계획은 공정만 다룬다. 자재는 두지 않는다.** 서버에 계획 자재와 자재 비교가 있지만,
자재 비교의 실적 자리에 자재량이 아니라 **확정된 작업 수량**이 들어간다 — 계획
자재(EA)를 시공 물량(개소)으로 나눈 비율이라 숫자는 나와도 뜻이 없다. 자재 소요량을
따로 재기 시작하면 그때 되살린다.

**계획 대비 현황은 공종 단위다.** 계획은 위치·작업내용까지 적지만 실적은 공종까지만
되짚을 수 있어 서버가 공종으로 합쳐서 준다. 그래서 그 표에는 위치 열이 없다.
단위도 계획 줄이 아니라 **공종에 등록된 `unit`**에서 온다 — 우리는 공종을 이름만
등록하므로 지금은 늘 비어 `-`로 뜬다.

**계획 한 줄은 공종 하나를 가리킨다.** 서버가 `workTypeId`를 필수로 받아 빈 줄을 먼저
만들어둘 수 없다. 그래서 `줄 추가`는 화면에만 줄을 깔고, 사람이 공종을 고르는 순간
등록된다(`PlanPage`의 `change`). 공종 칸이 자유 입력이 아니라 목록에서 고르는 이유이기도
하다 — 등록되지 않은 이름이 들어오면 비교에서 짝을 못 찾는다.

## 스택

- React 19 + TypeScript + Vite, react-router(`createBrowserRouter`), TanStack Query
- UI는 antd 6 + `@ant-design/icons`. antd API가 헷갈리면 antd MCP(`.mcp.json`)로 확인한다
- 엑셀 파일은 서버가 만든다(`src/api/exports.ts`). 프론트에 엑셀 라이브러리는 없다
- 날짜는 dayjs(`ko` 로캘)
- `@/` 는 `src/` 별칭. Prettier: 세미콜론 없음, 작은따옴표, printWidth 100

## 현재 상태

- **백엔드 연동이 모듈 단위로 진행 중이다.** `src/api/*.ts` 가 경계이고, 화면은 이 함수만
  부르므로 안쪽이 목이든 HTTP든 모른다. 붙은 것과 안 붙은 것:
  - 실제 HTTP(`src/lib/api.ts`): `exports.ts`(엑셀 두 개), `workTypes.ts`(공종 4개),
    `projects.ts`(목록·단건·생성·삭제), `photos.ts`(업로드·목록·확정), `aggregation.ts`,
    `plans.ts`(계획 공정 4개), `comparison.ts`(공정 비교)
  - **목 데이터는 남아 있지 않다.** `src/mocks/` 를 통째로 지웠다
  - 서버 응답을 우리 모양으로 옮기는 일은 `src/api/photos.ts`의 `toPhoto` 한 곳이 한다 —
    업로드·목록·확정이 모두 같은 `PhotoUpload`를 돌려주기 때문이다
  - 서버에 아직 없는 것: **사진 삭제**(`removePhoto`가 사실대로 거절한다),
    **작업일 목록**(그래서 작업일 드롭다운이 비어 있다),
    **항목 추가**(확정은 이미 있는 `itemId`만 고친다 — 사람이 새로 만든 줄은 못 보낸다)
  - 서버에 `POST /projects/{id}/excel/convert`(타사 양식 엑셀 변환)가 있지만 **쓰지 않는다.**
    없애기로 한 기능이 아직 안 지워진 것뿐이다 — 붙이지 말 것
- 서버 주소는 `VITE_API_BASE_URL` 이다. `.env.example` 을 복사해 `.env` 를 만들어야 하고,
  없으면 요청이 vite 자기 자신으로 가서 전부 404가 난다.
  **git worktree 는 `.env` 를 공유하지 않는다** — 워크트리마다 따로 만든다
- 서버가 채번한 id는 정수다(`WorkType.id`, `AggregationRow.workTypeId`).
  프로젝트 id만 문자열로 받아 쓴다 — 늘 주소창을 거쳐 다녀서(`/projects/:projectId/...`)
  `useParams`가 주는 값과 모양을 맞춘다(`src/api/projects.ts`의 `toProject`)
- `ConfigProvider`(`src/app/providers.tsx`)에 한국어 로캘과 `src/app/theme.ts`의
  디자인 토큰이 연결돼 있다. `cssVar` 모드라 토큰이 `--ant-*` CSS 변수로도 나간다

## UI 작업 규칙

**데스크톱 전용이다.** `src/index.css` 가 `#root { min-width: 1024px }` 로 못박아 두었고,
좁은 창에서는 가로 스크롤을 허용한다. 모바일 반응형 작업은 하지 않는다.

### 디자인 방향 — 엑셀처럼 보이게 한다

사용자가 하루 종일 엑셀을 쓰는 사무·공무 담당자이고 결과물 자체가 엑셀 사진대지라,
화면과 출력물이 같은 인상을 줘야 한다.

- **격자가 주인공이다.** 표는 `@/components/DataTable`을 쓴다 (`bordered` + `size="small"`이
  기본값). antd `Table`을 직접 쓰지 않는다 — 화면마다 옵션을 반복하면 언젠가 하나가 빠진다
- 모서리는 각지게. `borderRadius`는 2 이하
- 수량 열은 `@/components/columns`의 `numberColumn()`을 쓴다 (우측 정렬, 없는 값은 `-`)
- 색은 회색 계열이 기본이다. 녹색(`colorPrimary`)은 저장·생성·내려받기처럼 사용자가
  결과를 만드는 동작에만 붙인다
- 히어로 섹션, 큰 여백, 연출된 모션은 쓰지 않는다

**색·간격·타이포는 `src/app/theme.ts` 한 곳에서만 정의한다.**

- 컴포넌트(tsx)에서는 `theme.useToken()`으로 읽는다. 색상 리터럴은 ESLint가 막는다
- antd 토큰에 대응이 없는 개념(사진대지 양식 고유색 등)은 `theme.ts`가 export 한 상수를 쓴다
- CSS 모듈에서는 `var(--ant-color-border)` 같은 CSS 변수를 쓴다 (`cssVar` 모드)
- **새 antd 컴포넌트에는 전역 토큰이 자동으로 적용된다.** antd는 각 컴포넌트의 고유
  토큰을 `prepareComponentToken(token)`으로 전역 토큰에서 파생시킨다. 아직 한 번도 안 쓴
  컴포넌트도 이미 반경 2, 폰트 12, 높이 28, 녹색 주색으로 나온다.
  `theme.ts`의 `components`는 그 파생 결과가 우리 방향과 어긋나는 것만 손보는 자리다
  (예: `Table.headerBg`는 antd 기본 회색이 우리가 원한 회색이 아니라 덮었다).
  새 컴포넌트를 넣을 때 먼저 그냥 써보고, 어긋나는 값이 있을 때만 오버라이드를 추가한다.
  토큰 키 이름은 antd MCP의 `antd_token`으로 확인한다
  (MCP는 antd 기본값을 알려줄 뿐 우리가 정한 방향은 모른다)

**UI 문구는 한국어다.** 상태 라벨은 `src/lib/constants.ts`에 모여 있고
(`PHOTO_STATUS_LABEL`, `REVIEW_STATUS_LABEL`, `COMPARE_STATUS_LABEL`),
새 상태 문구도 여기에 둔다. 화면마다 같은 개념을 다른 말로 부르지 않는다.

### frontend-design 스킬의 적용 범위

이 저장소에서는 `frontend-design` 스킬을 다음 범위로만 적용한다.

적용한다:

- UX 라이팅 전반 — 시스템 용어가 아닌 사용자 용어, 능동태, 버튼과 결과 토스트의
  어휘 일치, 에러는 사과 대신 원인과 다음 행동을 밝힐 것, 빈 화면은 할 일을 제시할 것
- 토큰 규율과 자가 비평 패스
- 품질 바닥선 중 키보드 포커스 가시성과 `prefers-reduced-motion`

적용하지 않는다:

- 히어로 섹션, 시그니처 요소, "미적 리스크 하나 감수" 조항, 연출된 모션
- 모바일 반응형 (위 데스크톱 전용 규칙이 우선한다)

데이터 밀도가 높은 업무 화면이라 스킬이 말하는 "minimal direction"에 해당한다.
과감함이 아니라 간격·타입·정렬의 정밀함으로 품질을 낸다.
소개 페이지나 로그인 화면처럼 성격이 다른 화면이 추가되면 그때 이 범위를 다시 정한다.
